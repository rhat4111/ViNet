import {StyleSheet} from 'react-native';
import {COLORS} from '@/themes';

export default StyleSheet.create({
  container: {
    flex: 1,
    height: 2,
    backgroundColor: COLORS.BG_GRAY_3,
  },
});
